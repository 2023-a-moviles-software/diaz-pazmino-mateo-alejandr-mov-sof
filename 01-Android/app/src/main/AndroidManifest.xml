<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mateoapp.com.appmoviles01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FRecyclerView"
            android:exported="false" />
        <activity
            android:name=".ECrudEntrenador"
            android:exported="false" />
        <activity
            android:name=".CIntentExplicitoParametros"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BListView"
            android:exported="false" />
        <activity
            android:name=".AACicloVida"
            android:exported="false"
            android:label="@string/title_activity_aaciclo_vida"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ACicloVida"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>